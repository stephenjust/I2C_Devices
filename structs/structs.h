#ifndef _STRUCTS_H_
#define _STRUCTS_H_

// Used for storing comparison values to ensure duplicate printing is not completed.
typedef struct history {
	int n;
	int o;
};

// For storing circle data
typedef struct circle {
	int x;
	int y;
	int r;
};

#endif
